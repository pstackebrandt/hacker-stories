/**
 * @typedef {Object} PageTitleConfig
 * @property {string} title - The title to display
 * @property {string} subtitle - The subtitle to display
 */

/**
 * Page title configuration data.
 * The object is frozen to prevent modifications.
 * @type {PageTitleConfig}
 */
export const titleData = Object.freeze({
  title: "Your Hacker Stories",
  subtitle: "Get your stories from hacker news",
});